services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8096:8096"
    volumes:
      - ./backend/sqlite:/app/sqlite
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    environment:
      - VITE_API_URL=
    depends_on:
      - backend
    restart: unless-stopped
